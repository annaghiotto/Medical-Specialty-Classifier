services:
  ui:
    build:
      context: .
      dockerfile: Dockerfile
    image: medspec-ui:cpu
    ports:
      - "8501:8501"
    environment:
      - HF_HOME=/root/.cache/huggingface
    volumes:
      - ./artifacts:/app/artifacts
      - hf_cache:/root/.cache/huggingface

volumes:
  hf_cache: